'use strict';

/* Holding common stuffs for facebook endpoints */

const fbGraphApiEndpoint = `https://graph.facebook.com`;

module.exports = {
    fbGraphApiEndpoint,
    defaultFbApiVersion: 'v2.11'
}